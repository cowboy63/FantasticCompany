using UnityEngine;
using System.Collections;

namespace SGE
{
    namespace Entity
    {
        public class EntityLayers : MonoBehaviour
        {
            [SerializeField]
            private EntityLayerSystem system;
            [SerializeField]
            protected string initializer;
            // Use this for initialization
            void Start()
            {

            }

            // Update is called once per frame
            void Update()
            {

            }
        }
    }
}
